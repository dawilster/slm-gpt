.venv/
.env
node_modules/
__pycache__/
*.pyc
.DS_Store

# Compiled by scripts/build-runtime.sh — bundled into the Mac app at build time.
/halo-runtime

# Built by scripts/fetch-python-mlx.sh — relocatable Python + mlx-lm
# + fastapi, bundled into the Mac app's Resources/python-runtime/.
# ~363MB, regenerable, never committed. Documentation for what's
# inside lives in scripts/python-runtime.md.
/python-runtime/

# Cached download tarballs (Python interpreter, etc.).
/vendor/

# Bun build transients
.*.bun-build
*.js.map

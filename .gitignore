.venv/
.env
node_modules/
__pycache__/
*.pyc
.DS_Store

# Compiled by scripts/build-runtime.sh — bundled into the Mac app at build time.
/halo-runtime

# Fetched by scripts/fetch-swiftlm.sh — SwiftLM binary + mlx.metallib
# bundled into the Mac app's Resources/swiftlm-runtime/.
/swiftlm-runtime/
/vendor/

# Bun build transients
.*.bun-build
*.js.map
